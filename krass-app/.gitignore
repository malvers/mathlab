# Node
node_modules/

# Vosk German model (~45 MB) — placed locally into the Android assets, never committed
# (public repo + size). Download instructions are in README.md.
android/app/src/main/assets/model/

# Capacitor build output
android/app/build/
android/build/
