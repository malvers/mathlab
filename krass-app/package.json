{
  "name": "krass-zaehler",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Capacitor app: counts how often you say \"KRASS\" per day. On-device, offline, keyless (Vosk). Native foreground service does the listening; the web UI is just a dashboard.",
  "scripts": {
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
