{
  "name": "cv-pdf-tools",
  "private": true,
  "type": "module",
  "description": "Local tooling for generating the CV PDFs (see cv-pdfs.command)",
  "dependencies": {
    "puppeteer-core": "^25.5.0"
  }
}
