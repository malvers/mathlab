<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Stufe B: Solita's Vosk wake-word foreground service. -->
        <service
            android:name=".SolitaVoiceService"
            android:exported="false"
            android:stopWithTask="true"
            android:foregroundServiceType="microphone" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- isOnline() for the live-URL cache bypass -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" /> <!-- in-app APK self-update -->


    <!-- Location for the tracker. The foreground-service + notification permissions
         (FOREGROUND_SERVICE[_LOCATION], POST_NOTIFICATIONS) and the service itself are
         contributed by the background-geolocation plugin via manifest merge.
         ACCESS_BACKGROUND_LOCATION is intentionally omitted: the plugin uses a foreground
         service, so recording continues with the screen off without the stricter
         "Allow all the time" permission flow. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Activity Recognition: detect travel mode (walk / run / bike / vehicle) via Play Services. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Camera for the Foto-Spur (snap → pin + Gemini). Declared so @capacitor/camera
         requests it at runtime; capture-only (source CAMERA, no gallery) → no media perms. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Microphone for the Voice-Spur (record short audio notes pinned to the track). The Capacitor
         WebView grants the getUserMedia() audio request once this runtime permission is present;
         the first recording prompts for it. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Stufe B: Solita's Vosk wake-word foreground service (microphone, on-device). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Bring Solita to the foreground OVER the lock screen on a background Vosk wake (full-screen Intent). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Wake the screen so the activity / full-screen Intent is actually visible after a wake. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
