{
  "name": "voicerecorder-app",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor-Wrapper für den Voice Recorder — lädt docalvers.de/voicerecorder/voicerecorder.html live (Plan A). Native Spracherkennung via @capacitor-community/speech-recognition (iOS SFSpeechRecognizer, Android SpeechRecognizer).",
  "scripts": {
    "web": "./sync-web.sh",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor-community/speech-recognition": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
